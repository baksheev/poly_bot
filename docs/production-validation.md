# Production validation ledger

Last reviewed: 2026-07-17

Status: historical component-canary ledger. Current production decisions and
global invariants are defined in `docs/rust-production-architecture.md`.

This ledger records the manual canary required before each execution component
can be enabled in the autonomous runtime. A successful unit or integration test
does not replace a real operation against the production endpoint.

## Safety envelope

- DEX/CEX test inventory is limited to 100 USDC plus the corresponding WLD.
- ETH purchased only for gas is limited to 200 USDT, matching the explicitly
  approved bootstrap operation; canaries should use materially less.
- Binance orders use deterministic `rustval...` client IDs and are reconciled
  through a signed status/history request.
- Binance withdrawals may target only the address derived from
  `EVM_WALLET_PRIVATE_KEY`. The manual CLI must not accept a destination address.
- The test wallet private key lives in Secret Manager and must never appear in
  command arguments, logs, telemetry, source, or this ledger.
- Every mutating command requires its exact live-confirmation phrase and
  enforces its cap before opening an authenticated trading, signing, or
  withdrawal connection.
- Production trading credentials must remain isolated from Rails and may be
  enabled only after the full launch gate passes.

## Evidence

| Component | Production operation | Status | Evidence |
| --- | --- | --- | --- |
| Binance subaccount | signed account and WLDUSDC commission hydration from diagnostic VM | Passed | Spot, `canTrade=true`, two nonzero balances, WLD and USDC present; static egress `34.143.148.4` |
| Binance capital routes | signed `capital/config/getall` from diagnostic VM | Passed | WLD direct and Optimism deposit/withdrawal available; USDC Optimism deposit/withdrawal available and no direct route |
| Dedicated subaccount order audit | signed `allOrders` query | Passed | independently returned all four 2026-07-17 `rustval...` canary orders with matching IDs, sides, types, quantities and `FILLED` status |
| Dedicated subaccount Spot WS API | capped MARKET buy/sell WLDUSDC | Passed | BUY order `455789048`: `26.2 WLD` for `9.96386 USDC`; SELL order `455789056`: `26.2 WLD` for `9.96124 USDC`; final WLD exactly matched the starting balance |
| Pre-isolation Binance Spot WS API | MARKET buy WLDUSDC | Historical | order `455189372`, `FILLED`, 24.5 WLD / 9.96415 USDC; does not validate the dedicated subaccount |
| Pre-isolation Binance Spot WS API | MARKET sell WLDUSDC | Historical | order `455189375`, `FILLED`, 24.5 WLD / 9.95925 USDC; does not validate the dedicated subaccount |
| Pre-isolation Binance reconciliation | signed `allOrders` query | Historical | both deterministic `rustval...` IDs returned `FILLED`; rerun on the dedicated subaccount |
| EVM signer | derive public address from Secret Manager value | Passed | `0x90D990C81320221D2882De32beeA78923c1e77A3` |
| World Chain RPC | chain ID, latest block, pending nonce, ETH/WLD/USDC | Passed | chain `480`; native balance `0.007982721314804481 ETH` after Across fill |
| Optimism RPC | chain ID, latest block, pending nonce, ETH/WLD/USDC/USDC.e | Passed | chain `10`; native balance `0.001997329279441474 ETH`; pending nonce `1` |
| Binance LIMIT IOC | capped full-fill buy/sell WLDUSDC | Passed | BUY order `455788994`: `26.1 WLD` for `9.92583 USDC`; SELL order `455788998`: `26.1 WLD` for `9.92061 USDC`; both independently reconciled as `FILLED` |
| Binance IOC partial fill | cancelled and filled quantity reconciliation | Pending | full-fill IOC path passed; a controlled partial-fill canary is still required |
| Binance recovery | LIMIT unwind, then MARKET loss fixation | Pending | — |
| Binance withdrawal | ETH to Optimism | Passed | `0.01 ETH` requested; `0.009985 ETH` received after Binance fee |
| Binance withdrawal | WLD direct to World Chain | Passed | exercised by the recoverable Binance-to-wallet rebalance route |
| Binance fallback | WLD to Optimism when World route is unavailable | Passed | fallback withdrawal completed and was bridged to World Chain |
| Across native ETH | Optimism to World Chain | Passed | origin `0x8a6d9da68dd5b9ed9f4bbcc6e7d736f8d249a773e8b9921003eeceb89eb57f86`; fill `0x09bcd6beb4ed5f188df7cc3b2b23f7d215a95111c05bfef390cf3632adfb7877`; sent `0.00798759334452456 ETH`; received `0.007982721314804481 ETH`; retained more than 20% on Optimism |
| Across USDC | Optimism to World Chain | Passed | production rebalance Binance to wallet completed |
| Across USDC | World Chain to Optimism | Passed | production rebalance wallet to Binance completed and Binance credited the deposit |
| DEX Uniswap V3 | 10 USDC to WLD | Passed | swap `0xc56005476e0acf9b0f1bf6dbb3c05be11b5fb6f90f7fd2a9a962a95305b985c3`; received `26.118979277000460343 WLD`; gas used `132,596`; Rails-compatible limit `850,000` includes `50,000` explicit additional gas |
| DEX Uniswap V3 | exact purchased WLD to USDC | Passed | recovery swap `0xf196478dd5c1e435b5c3254413feb6df34a96ed99dccd45abbbcab43c76527fc`; sold `26.118979277000460343 WLD`; received `9.940091 USDC`; gas used `125,888` |
| DEX Uniswap V4 | 10 USDC to WLD | Passed | swap `0xb9dcd46ec62ee73f01c2c6e83e4ebf1e5c2f385b7083b348287d6a9515032e0b`; received `26.105429068306941392 WLD`; gas used `135,247`; Rails-compatible limit `599,952` includes `50,000` explicit additional gas |
| DEX Uniswap V4 | exact purchased WLD to USDC | Passed | swap `0x77211148b5abd9384b3749201376bca1bbee689c2c9cc4a81f7684e5383c5fe8`; sold `26.105429068306941392 WLD`; received `9.801022 USDC`; gas used `127,771`; final WLD balance exactly matched the pre-canary balance |
| EVM recovery | reverted/replaced transaction and nonce recovery | Pending | — |
| Concurrent execution | DEX and CEX legs start concurrently | Pending | — |
| Compensation | DEX failure unwinds CEX | Pending | — |
| Compensation | CEX failure retries MARKET and records loss | Pending | — |
| Rebalance | WLD direct route end-to-end | Passed | Binance-to-wallet withdrawal and wallet-to-Binance deposit both completed |
| Rebalance | USDC and WLD Optimism + Across fallback end-to-end | Passed | all four fallback direction/token combinations completed; final WLD operations were `rebalance-27-6022301e0756c887` and `rebalance-37-ba77134a174e0d45` |
| Rebalance settlement | fresh Binance and wallet snapshots after completion | Passed | the normal planner restarted with no action after both streams advanced; final balances are recorded in `docs/rebalancing.md` |
| Rebalance monitoring | Cloud Monitoring email alerting | Passed | one-minute structured heartbeat plus immediate fault and five-minute missing-heartbeat policies are enabled for `baksheev@me.com`; a synthetic fault matched the log metric |
| GKE subaccount preflight | trading key, signer, caps, isolated inventory | Passed | GKE NAT authenticated the isolated subaccount against exactly 1,000 USDC and 2,500 WLD; WLD direct/fallback and USDC fallback routes pass; signer and both RPCs pass; GitHub caps are 500 USDC and 1,250 WLD. |
| GKE first full-live attempt | 500 USDC fallback intent | Failed closed | `localentity` withdrawal returned HTTP 401 / Binance `-1002`; durable journal remained at `intent_recorded`, wallet nonce journal stayed empty, and no withdrawal was indexed. This was an API-key permission failure, not evidence that the endpoint was wrong. |
| GKE standard withdrawal retry | 500 USDC fallback intent | Failed closed | The experimental `capital/withdraw/apply` request also returned HTTP 401 / Binance `-1002` before any withdrawal was indexed, proving the shared API key lacked withdrawal authorization even though the Spot account reported `canWithdraw=true`. Startup now checks `account/apiRestrictions` before creating a journal. |
| GKE master-treasury preflight | master key permissions and subaccount mapping | Passed | Revision `e077b2bab2a9498352afc12a3fdedb87237845f8` verified Reading, Withdrawals, Universal Transfer, the production IP allowlist, and the master's exact WLD/USDC view of the configured subaccount before opening journals. |
| GKE master internal transfer | 500 USDC subaccount Spot to master Spot | Failed closed | Binance rejected `universalTransfer` with HTTP 401 / `-1002`; the journal contains only the deterministic `intent_recorded` event, the wallet journal is empty, and no withdrawal was submitted. The endpoint also requires the distinct `enableInternalTransfer` API-key flag; startup now checks both transfer flags before opening journals. |
| Binance ESP address verification | exact Arbitrum Satoshi deposit test | Verified | Binance's authenticated read-only Address Verification API first returned exactly one matching `USDC` / `ARBITRUM` record for the production wallet with status `PENDING`, owner flag `1`, and Satoshi verification method `1`. Revision `d1fc9499b53f9892320aa2a37db530b4ed6649b1` then sent the one approved direct transfer of exactly `0.9987 USDC` to Binance deposit address `0x64d62673799a8dc69825ff1cc0d624b1065dab39`. Arbitrum transaction `0xff9bf15b5ddc745eb50c4c4e77d746b4ec1e9e84facfce6cd92e948c20c85c45` has receipt status `1` and its sole USDC Transfer credit is exactly `998700` base units; source and recipient post-state balances were `24.0013` and `0.9987 USDC`. Binance subsequently returned the exact record as `VERIFIED` before the prefunding executor was allowed to reconcile or submit a withdrawal. The durable operation prevents a second transfer and bridge remained forbidden. |
| Binance rejected ESP withdrawal reconciliation | exact prior local-entity incident | Failed closed, superseded | The exact legacy row is `trId=67181540`, `travelRuleStatus=4`, with omitted `withdrawalStatus` and no transaction hash. Recovery refetches the exact row, rejects identity changes or broadcast/completed evidence, proves capital history empty, and validates the durable `401.2 ESP` subaccount-to-master transfer. The initial source review copied Rails commit `6520658`; the later full-size ESP production rejection proved that global local-entity endpoint change was itself a regression. Deposit questionnaires remain conditional and separate. |
| Binance M9 USDC standard probe | exact durable 3 USDC operation | Historical fail-closed recovery | Operation `rebalance-272-c36484318ac04bca` has internal transfer `395795295053` in `SUCCESS`, its original standard request had no indexed outcome, and the wallet/address evidence remained exact. Its previously approved recovery is historical; it no longer exists in the active artifact and cannot select local-entity for a new withdrawal. |
| Binance manual ESP capital withdrawal | exact Binance and Arbitrum transaction | Reconciled | The operator used the ordinary Binance capital withdrawal flow. Transaction `0xc65237273346c647f2e47e04ad67b81e7002eedf6da779d04a5b3c49e2fd129b` credited exactly `400 ESP` to `0x90D990C81320221D2882De32beeA78923c1e77A3` on Arbitrum with a `1.2 ESP` fee. Init recovery matched the exact Binance history row, successful ERC-20 receipt, address, fee, zero pre-balance, wallet post-balance, and exhausted master inventory before completing durable operation `rebalance-268-15f59bc55dcaed54`. It then observed both `26.8013 USDC` and `400 ESP` above their targets with `transfer_count=0`; no duplicate transfer or withdrawal was submitted. |
| Binance zero-balance omission | first exact manual ESP recovery rollout | Failed closed, corrected | The recovery matched the reviewed Binance withdrawal and Arbitrum receipt, then stopped because `/api/v3/account` omitted the fully exhausted ESP row instead of returning explicit zeroes. The account parser already treats missing assets as zero in cross-account comparisons; manual recovery now uses the same rule and has a regression test. No journal transition or external submission occurred before rollback. |
| Arbitrum M9 first allowance | exact pre-broadcast fee-cap rejection | Failed closed, corrective recovery | Init funding completed, but the first ESP router allowance transaction `0xbdfaa80920ebd8513a01d9a368f581ae8b552e8f4528be54586eeb0963079977` was rejected by RPC before broadcast: `maxFeePerGas=20,102,000`, `baseFee=20,148,000`, nonce `1`. Startup subsequently blocked the signed-but-absent nonce as designed. The corrective artifact binds recovery to the exact operation/hash/chain/wallet/nonce, requires two absent transaction/receipt observations with unchanged `latest=pending=1`, adds a `12,000 bps` Arbitrum max-fee envelope, and preserves fail-closed handling for every non-specific unknown outcome. |
| Arbitrum M9 recovery identity preflight | exact production journal scope | Failed closed, corrected before retry | Corrective rollout `1a60adaa16b11e01e73091dc8c868c666bb0ed54` made no transfer or transaction because its reviewed recovery used the unqualified wallet id `evm-wallet:primary`, while the production journal identity is `eip155:42161:evm-wallet:primary`. The init guard rejected the mismatch and the workflow rolled back. Recovery and live startup now share one network-qualified scope constructor, live startup asserts it against the compiled ownership graph, and the predeploy test loads the checked-in production bundle and proves its exact Arbitrum network and wallet-location IDs. |
| Arbitrum M9 first parent | bounded DEX-first ESP/USDC operation | Filled, restart bug corrected | Parent `paper-1785426526104975-74237350-a672e89a68093aa2-ds` spent approximately 10 USDC on Arbitrum in transaction `0x09f9a7da4b5f5d704a8d68746f788264340af631d4ad3703acc8887251546ea9` (nonce `3`, receipt status success) and the primary Binance IOC sold exactly `134 ESP` for `9.88786 USDC` in filled order `41614391`. The chain probe then compared the legitimate post-trade `16.860785 USDC` balance with the one-shot `25 USDC` prefunding target, degraded readiness, and a rejected next opportunity exposed that pre-admission failures could not publish a release event without a journaled parent. The task exited and restart correctly failed closed. Runtime token-presence minima are now versioned separately from immutable prefunding targets, exact inventory reservations remain the per-trade balance gate, pre-admission rejection events carry their original pair identity without requiring a journaled parent, and expected bounded-canary inventory exhaustion is informational rather than a production ERROR. |
| Arbitrum M9 post-parent allowance recovery | exact durable allowance and trade journals | Failed closed, corrected before retry | Corrective rollout `560168bd9511b1294ab9efba0aaebb3013877200` passed the reduced-balance readiness gate but stopped before a transaction because startup rebuilt the stable allowance operation IDs from changed post-trade USDC/ESP balances. The EVM journal rejected the changed calldata with `journaled DEX transaction does not match requested call`; the workflow rolled back and no approval was submitted. Startup now derives remaining USDC allowance authority from the durable cumulative M9 notional, caps ESP authority at the versioned bootstrap amount, uses amount-qualified operation IDs for any reviewed refill, and permanently locks allowance mutation without preparation when parent/failure/loss/time authority is exhausted. The production-derived fixture covers the exact `9.940515 USDC` admitted parent, `16.860785 USDC` post-trade wallet balance, `10.059485 USDC` remaining allowance authority, the production `balanced_profit` terminal state, failure stop, and rollout expiry. |
| Arbitrum M9 post-parent restart | exact revision `94b61e81f5ff44603208f9be8254811301250ca7`, CI `30562391510`, Deploy GKE `30562782095` | Passed | Immutable digest `sha256:9f9503be4de439f2f5d8c28bbce9019909e55b9893c61c7cfd29b573ef4085b5` started sole Pod `arb-bot-5848bf8957-rrghx` at `2026-07-30T16:51:24Z`; Ready at `16:51:42Z`, both containers restart-free, prefunding init exit `0`, and GCE remained `TERMINATED`. Init found the durable marker and refused a second funding run. Startup recovered one `9.940515 USDC` parent, locked allowance mutations without preparing a new approval, and emitted only expired-window M9 rejections. The exact window `[16:51:24Z,16:54:32Z)` reported `m9_gate=canary_observed`, readiness `3/3`, parent `1/2`, notional `9.940515/20 USDC`, zero telemetry drops and zero production ERROR. The only EVM broadcast was World Chain WLD operation `paper-1785430317024470-854487434-868f382e7611e465-ds-dex.swap` at nonce `1600`; no Arbitrum allowance, ESP trade, prefunding, or rebalance mutation was submitted. WLD socket/parse p99 were `41/7 µs`; fee-500 receive and total p99/max were `69/69 µs` and `128/128 µs`, below the frozen hard limits. CPU max was `0.0202` core, `nr_throttled=0`; cgroup memory peak was `76.7 MB` with no memory high/max/OOM event. |
| M10 first corrective rollout | revision `6f32bd6e07dc16aa8fad55c3fe03de748e754991`, CI `30599575695`, Deploy GKE `30599828375` | Failed closed, correction prepared | Pod `arb-bot-7b88fdcc7c-7x9nq` safely closed pre-transfer crash intent `rebalance-294-96fd53e70c1ab390` with delayed empty transfer history and no external mutation. It then reused the economic identity as operation `rebalance-296-96fd53e70c1ab390`, completed exact subaccount-to-master transfer `395924104268` for `1,197.503244 USDC`, and persisted withdrawal submission intent before the standard endpoint synchronously rejected it with HTTP 400 / `-4104`. Binance created no withdrawal, as separately confirmed by the operator; Optimism USDC remained at 508 base units and no M10 Arbitrum mutation started. Rails source review established that every withdrawal must use `localentity/withdraw/apply` with the inline self-wallet questionnaire. |
| M10 local-entity corrective rollout | revision `2c06cedf5ea6bf57d54dfad63783bed34602e48f`, CI `30601469715`, Deploy GKE `30601720697` | Failed closed, automatically rolled back | Immutable digest `sha256:ef51ce22f558f03aad266e5b28f4af5640865e3eb872ff665ea17ca3bfb78ddf` reached Pod `arb-bot-795c6cddd5-k7nqw`, but durable journal replay rejected the historical approved ESP terminal retry `Failed -> standard withdrawal submission intent` after the new runtime narrowed that exceptional transition to `local_entity`. The application exited before recovery or a new withdrawal; the workflow automatically restored digest `sha256:165acf1aec1fa9f0903aff5cf8d3d9b34ccfe7f635a904371518e1c684cae28f` as healthy Pod `arb-bot-7b88fdcc7c-pxjsj`. Its sole allowed reconciliation query found no indexed USDC withdrawal and durably advanced operation `rebalance-296-96fd53e70c1ab390` from query count zero to one. The follow-up keeps legacy `standard`/`travel_rule` terminal-retry transitions replay-only while all live appends remain pinned to `local_entity`, and binds the exact absent-withdrawal recovery to the exhausted query count. |
| M10 authoritative Arbitrum rebalance canary | revision `243aa47ca72399b169641f41cc9a19eb9ddef144`, CI `30602739221`, Deploy GKE `30602981046` | Passed | The exact production journal suffix and its only historical terminal retry were replayed in tests before release. Immutable digest `sha256:967744e99d82f3d14214ef126acd7a0406d8b31006598743254e81e8b52808c5` started sole Pod `arb-bot-6549d944b9-bnp7j` at `2026-07-31T04:07:02Z`; both containers remained Ready with zero restarts and GCE remained `TERMINATED`. Startup closed the exhausted rejected standard operation without an external mutation. M10 then created only operation `rebalance-302-4d88f5b3efc955fd`: internal transfer `395947861025`, Rails-compatible local-entity withdrawal reference `67272800`, exact `25 USDC` Binance debit, and `24.8 USDC` wallet credit after the `0.2 USDC` network fee. The fixed window `[04:07:02Z,04:22:20Z)` reported `m10_gate=canary_observed`, transfer `1/2`, active `0/1`, failures `0/1`, allocator/saga/settlement failures `0`, zero telemetry drops, zero production ERROR, and no new durable intent after the 15-minute stop time. CPU max was `0.01743` core, `nr_throttled=0`; cgroup memory peak was `57.6 MB` with no memory high/max/OOM event. The extended authoritative latency window `[04:07:02Z,04:37:10Z)` recorded WLD socket/parse p99 `47/8 µs`; fee-500 receive `n=116`, p99/max `127/154 µs`; build `n=107`, p99/max `21/30 µs`; total p99/max `35/37 µs`, with complete stage timing `107/107`. The sole `154 µs` receive outlier correlated with one bounded decision-owner DEX drain; after the required sample size, receive p99 was only `4.1%` above the frozen `122 µs` reference and both receive and total remained below hard limits. |
| M12 full calculated rebalance first rollout | revision `b825c99093a7b0f52a91a14abf1684f96ba2046b`, CI `30616849202`, Deploy GKE `30617198354` | Failed closed, corrective release pending | Immutable digest `sha256:7906d2df285f6cc9d708d1db3a08665267cbe1eada0c56c82ba93c87736ac783` started Pod `arb-bot-84859bf84f-jmrbv`. Operation `rebalance-318-3a3d0f0d9a8d37f5` completed a direct `685.619537 USDC` rebalance. Exact ESP operation `rebalance-324-8b62a7c14f4ef643` then completed only master transfer `396036135710` for `4,464.93818055 ESP`; the hard-coded local-entity endpoint synchronously rejected the withdrawal with HTTP `400`, code `-4024`, `[031031] User does not own this currency.` The durable operation became terminal failed, no withdrawal was broadcast, and read-only capital recovery at `2026-07-31T09:02:15Z` returned `matching_withdrawals=0` for `rb8b62a7c14f4ef6434a88c384bbb83c`. Corrective recovery is bound to that exact identity and reuses the master inventory through one standard `capital/withdraw/apply` submission without another internal transfer. |
| M12 standard endpoint correction | revision `d36c750a05608f0ffac865a5c0932e25d473bd7e`, CI `30619846146`, Deploy GKE `30620230922` | Failed closed, root cause identified | Immutable digest `sha256:d80d5a263b853ec665197229915cbfea5d9f10379f6f5418ca060b1930eba59f` reused master transfer `396036135710` and sent no second internal transfer. The standard endpoint synchronously returned HTTP `400` / `-4104` requiring Travel Rule and created unbroadcast `trId=67298920`. No capital withdrawal or blockchain transaction was created. Together with earlier unbroadcast local-entity `trId=67294348`, Binance history proved that neither a globally pinned standard endpoint nor the incomplete local-entity questionnaire matched the UI behavior. |
| M12 operator exact ESP withdrawal | Binance withdrawal `e02357b25de24e1ba9965bf524db37f7` | Completed, durable reconciliation pending | The operator withdrew the exact remaining master inventory through Binance UI with no additional questionnaire. Gross debit was `4,464.93818055 ESP`, fee `1.1 ESP`, and Arbitrum transaction `0x553d9635dab1477c6aab9a17fc4ab860040e44db8ca085cb894a6b3184bc27fd` credited exactly `4,463.83818055 ESP`. Travel Rule `trId=67300014` records the successful UI questionnaire as `isAddressOwner=1`, `sendTo=1`, `satoshiToken=USDC`, `verifyMethod=1`. Read-only chain state is exactly `4,998.761819437482447575 ESP`, equal to the journaled pre-balance plus that credit. The pending release can only reconcile these exact records and receipt; it contains no withdrawal or internal-transfer call. |
| M12 first exact receipt recovery | revision `24e86dd07d5bcebd15993ad7dfe44747fcf4b046`, CI `30623132230`, Deploy GKE `30623488312` | Failed closed before journal transition | Pod `arb-bot-55cd4c676f-gxbzn` started Ready and made no Binance or chain mutation, but the exact recovery queried by deterministic `withdrawOrderId` and Binance omitted that field on one of the two reviewed unbroadcast Travel Rule rows. The strict two-row count stopped recovery before receipt reconciliation. The correction selects only versioned trIds `67294348` and `67298920` from network-scoped history, still validates their complete economic identity/status, and retains the no-submit recovery contract. |

The V3 buy receipt initially raced an HTTP RPC node whose `latest` balance
snapshot was one block behind. The command failed closed before the sell; the
measured `26.118979277000460343 WLD` delta was then sold through the capped
recovery command. Balance verification now waits for a canonical block at or
above the receipt block and reads both ERC-20 balances at that same block. The
V4 round trip subsequently completed end to end through that corrected path.
All ten approval/swap operations in the canary journal end in
`mined_success`; final World Chain nonce is `15/15` with no pending transaction.

The failed-closed rows are retained because they verify important recovery
boundaries; later successful operations supersede them as route-readiness
evidence. The wallet and Binance hydration evidence intentionally records no
secret material or raw authenticated request. ERC-20 rebalance validation is
complete. Standalone DEX V3/V4 execution and fully filled dedicated-account
Binance LIMIT IOC/MARKET order placement are complete. Partial-fill,
unknown-outcome, User Data Stream, restart, and composed two-leg rows above
preserve the state of this historical canary ledger; they are not a current
statement that production is disabled. Current live behavior, accepted risk
boundaries, and remaining architecture debt are recorded in
`docs/rust-production-architecture.md`.
